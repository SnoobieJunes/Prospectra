# 2026-07-13 (P0): Core engine namespace — connectors, flows, sampling, stats/mining, LLM,
# scraper all land here in later phases. MUST stay importable headless: no Qt and no
# prospectra.ui imports (enforced by the import-linter contract in pyproject.toml).
